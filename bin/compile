#!/usr/bin/env bash

BDIR="$1"
CDIR="$2"

echo "$BDIR";
echo "$CDIR";
echo "-----------";
pwd;
echo "-----------";
ls -laph;
echo "-----------";
ls -laph $BDIR;
echo "-----------";
ls -laph $CDIR;
echo "OK.";
exit 0;
